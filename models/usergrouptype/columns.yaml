# ===================================
#  Column Definitions
# ===================================

columns:

    id:
        label: acornassociated.user::lang.group.id
        invisible: true

    name:
        label: acornassociated.user::lang.group.name
        searchable: true

    image:
        label: backend::lang.editor.image
        type: image

    groups_count:
        label: acornassociated.user::lang.models.usergrouptype.groups_count
        default: 0
        sortable: false

    colour:
        label: backend::lang.branding.primary_color
        type: colorpicker
